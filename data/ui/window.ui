<?xml version="1.0" encoding="UTF-8"?>
<interface>
  <requires lib="gtk" version="4.14"/>
  <requires lib="libadwaita" version="1.6"/>

  <menu id="primary_menu">
    <section>
      <item>
        <attribute name="label" translatable="yes">_Open…</attribute>
        <attribute name="action">win.open</attribute>
      </item>
      <submenu>
        <attribute name="label" translatable="yes">Open _Recent</attribute>
        <!-- Rebuilt from GtkRecentManager whenever the list changes; see ts_window_refresh_recent. -->
        <section id="recent_section"/>
      </submenu>
    </section>
    <section>
      <item>
        <attribute name="label" translatable="yes">_Export WAV…</attribute>
        <attribute name="action">win.export</attribute>
      </item>
    </section>
    <section>
      <item>
        <attribute name="label" translatable="yes"
                   comments="The ROM is the SCCore.dll the engine reads its tables from.">Import Sound Canvas _ROM…</attribute>
        <attribute name="action">win.import-rom</attribute>
      </item>
      <item>
        <attribute name="label" translatable="yes">_Preferences</attribute>
        <attribute name="action">app.preferences</attribute>
      </item>
    </section>
    <section>
      <item>
        <attribute name="label" translatable="yes">_About Tabula Sonora</attribute>
        <attribute name="action">app.about</attribute>
      </item>
    </section>
  </menu>

  <template class="TsWindow" parent="AdwApplicationWindow">
    <property name="title" translatable="yes">Tabula Sonora</property>
    <property name="default-width">700</property>
    <property name="default-height">720</property>
    <property name="width-request">360</property>
    <property name="height-request">294</property>

    <!--
      Height, not width: the wide layout stacks the transport above the mixer, so what runs out
      first is vertical room. A short window leaves the mixer a couple of rows tall and useless
      while the transport is still perfectly comfortable, which is exactly when the two should stop
      sharing the window and move into a switcher.

      The transport needs about 190px and a mixer wants several rows below it, so 460sp is where
      stacking stops paying. In sp rather than pixels, so the threshold follows the text scale.
    -->
    <child>
      <object class="AdwBreakpoint">
        <condition>max-height: 460sp</condition>
        <setter object="layouts" property="layout-name">narrow</setter>
        <setter object="switcher_bar" property="reveal">True</setter>
      </object>
    </child>

    <property name="content">
      <object class="GtkStack" id="screens">
        <property name="transition-type">crossfade</property>

        <!--
          Not homogeneous. A stack sizes itself to the largest of *all* its pages by default, so the
          first-run screen's identity table went on dictating the window's minimum width for the
          whole session, long after it stopped being visible. These two screens are shown at
          completely different times and have no reason to agree on a size.
        -->
        <property name="hhomogeneous">False</property>
        <property name="vhomogeneous">False</property>

        <!-- First run: no voice yet, so there is nothing to show but the way to get one. -->
        <child>
          <object class="GtkStackPage">
            <property name="name">setup</property>
            <property name="child">
              <object class="AdwToolbarView">
                <child type="top">
                  <object class="AdwHeaderBar">
                    <property name="show-title">False</property>
                    <child type="end">
                      <object class="GtkMenuButton">
                        <property name="icon-name">open-menu-symbolic</property>
                        <property name="primary">True</property>
                        <property name="tooltip-text" translatable="yes"
                                  comments="Tooltip on the hamburger button.">Main Menu</property>
                        <property name="menu-model">primary_menu</property>
                      </object>
                    </child>
                  </object>
                </child>
                <property name="content">
                  <object class="TsRomSetup" id="rom_setup"/>
                </property>
              </object>
            </property>
          </object>
        </child>

        <!-- The player proper. -->
        <child>
          <object class="GtkStackPage">
            <property name="name">player</property>
            <property name="child">
              <object class="AdwToolbarView">
                <child type="top">
                  <object class="AdwHeaderBar">
                    <property name="title-widget">
                      <object class="AdwWindowTitle" id="window_title">
                        <property name="title" translatable="yes">Tabula Sonora</property>
                      </object>
                    </property>
                    <child type="start">
                      <object class="GtkButton">
                        <property name="icon-name">document-open-symbolic</property>
                        <property name="tooltip-text" translatable="yes"
                                  comments="Tooltip on the button that opens the file chooser.">Open a MIDI file</property>
                        <property name="action-name">win.open</property>
                      </object>
                    </child>
                    <child type="end">
                      <object class="GtkMenuButton">
                        <property name="icon-name">open-menu-symbolic</property>
                        <property name="primary">True</property>
                        <property name="tooltip-text" translatable="yes"
                                  comments="Tooltip on the hamburger button.">Main Menu</property>
                        <property name="menu-model">primary_menu</property>
                      </object>
                    </child>
                    <!--
                      A header button rather than a menu item: it is asked of the file that is
                      playing, repeatedly, the way Open is asked once. Only on this screen, because
                      the setup screen has no song to ask about. It greys out with the song in
                      ts_window_update_screen, alongside Export.
                    -->
                    <child type="end">
                      <object class="GtkButton">
                        <!--
                          The circled "i", not dialog-information-symbolic: that one is a light
                          bulb in Adwaita, which reads as a hint or a tip rather than as facts
                          about the file being played.
                        -->
                        <property name="icon-name">help-about-symbolic</property>
                        <property name="tooltip-text" translatable="yes"
                                  comments="Opens the window describing the loaded file.">Song information</property>
                        <property name="action-name">win.song-info</property>
                      </object>
                    </child>
                  </object>
                </child>

                <child type="bottom">
                  <object class="AdwViewSwitcherBar" id="switcher_bar">
                    <property name="stack">panes</property>
                  </object>
                </child>

                <property name="content">
                  <!--
                    One transport and one mixer, built once and moved between layouts. Two separate
                    widget trees would mean two of each, and two sets of state to keep in step.
                  -->
                  <object class="AdwMultiLayoutView" id="layouts">

                    <child>
                      <object class="AdwLayout">
                        <property name="name">wide</property>
                        <property name="content">
                          <object class="GtkBox">
                            <property name="orientation">vertical</property>
                            <child>
                              <object class="AdwLayoutSlot">
                                <property name="id">transport</property>
                              </object>
                            </child>
                            <child>
                              <object class="GtkSeparator"/>
                            </child>
                            <child>
                              <object class="AdwLayoutSlot">
                                <property name="id">mixer</property>
                              </object>
                            </child>
                          </object>
                        </property>
                      </object>
                    </child>

                    <child>
                      <object class="AdwLayout">
                        <property name="name">narrow</property>
                        <property name="content">
                          <object class="AdwViewStack" id="panes">
                            <child>
                              <object class="AdwViewStackPage">
                                <property name="name">transport</property>
                                <property name="title" translatable="yes"
                                          comments="View switcher page holding the transport, on short windows.">Playing</property>
                                <property name="icon-name">media-playback-start-symbolic</property>
                                <property name="child">
                                  <object class="AdwLayoutSlot">
                                    <property name="id">transport</property>
                                  </object>
                                </property>
                              </object>
                            </child>
                            <child>
                              <object class="AdwViewStackPage">
                                <property name="name">mixer</property>
                                <property name="title" translatable="yes"
                                          comments="View switcher page holding the mixer strips.">Mixer</property>
                                <property name="icon-name">view-list-symbolic</property>
                                <property name="child">
                                  <object class="AdwLayoutSlot">
                                    <property name="id">mixer</property>
                                  </object>
                                </property>
                              </object>
                            </child>
                          </object>
                        </property>
                      </object>
                    </child>

                    <property name="layout-name">wide</property>
                  </object>
                </property>
              </object>
            </property>
          </object>
        </child>
      </object>
    </property>
  </template>
</interface>
